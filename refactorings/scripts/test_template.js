const { REFACTORING_NAME } = require('../lib/REFACTORING_NAME/REFACTORING_NAME');
const { expect } = require('chai');
const { describe, it } = require('mocha');

describe('REFACTORING_NAME', () => {
  it('', () => {
    expect(true).equal(true);
  });
});
