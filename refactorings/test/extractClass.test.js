// const { extractClass } = require('../extractClass/extractClass');
const { expect } = require('chai');
const { describe, it } = require('mocha');

describe('extractClass', () => {
  it('', () => {
    expect(true).equal(true);
  });
});
